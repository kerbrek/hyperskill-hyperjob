---
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: hyperjob-web
    volumes:
      - static_files:/app/static
    env_file:
      - ./ENV/.env.app
    depends_on:
      - db

  db:
    image: postgres:13
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - ./ENV/.env.db

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    image: hyperjob-nginx
    volumes:
      - static_files:/app/static/:ro
    ports:
      - "8000:80"
    depends_on:
      - web

volumes:
  postgres_data:
  static_files:
